{
    "builds": [
        {
            "src": "api/main.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/budget/(.*)",
            "dest": "api/main.py"
        }
    ]
}